---
import { homeKpis } from '@/data/site';

interface Props {
  kpis?: typeof homeKpis;
  background?: 'page' | 'surface-subtle';
}

const { kpis = homeKpis, background = 'page' } = Astro.props;
const bgClass = background === 'surface-subtle' ? 'bg-bg-surface-subtle' : 'bg-bg-page';
---

<section class={bgClass}>
  <div class="max-w-layout mx-auto px-md md:px-2xl py-3xl">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-xl">
      {kpis.map(kpi => (
        <div class="space-y-2xs">
          <p class="text-5xl md:text-display-l font-bold text-fg-brand">{kpi.num}</p>
          <p class="text-body-m font-medium text-fg-secondary max-w-[240px]">{kpi.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>
